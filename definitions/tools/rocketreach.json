{
  "name": "rocketreach",
  "type": "service",
  "title": "RocketReach",
  "category": "Sales",
  "description": "RocketReach as a tool (contact enrichment + prospecting; API key)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "api_key"},
        "inject": {"type": "env", "env_var": "ROCKETREACH_API_KEY"}
      }
    ]
  }
}
