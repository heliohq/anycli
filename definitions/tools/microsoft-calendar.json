{
  "name": "microsoft-calendar",
  "type": "service",
  "title": "Outlook Calendar",
  "category": "Productivity",
  "description": "Microsoft Outlook Calendar as a tool (user access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "MICROSOFT_CALENDAR_ACCESS_TOKEN"}
      }
    ]
  }
}
