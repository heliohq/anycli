{
  "name": "microsoft-onedrive",
  "type": "service",
  "description": "Microsoft OneDrive as a tool (user access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "MICROSOFT_ONEDRIVE_ACCESS_TOKEN"}
      }
    ]
  }
}
