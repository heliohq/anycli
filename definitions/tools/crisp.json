{
  "name": "crisp",
  "type": "service",
  "description": "Crisp customer support as a tool: website inbox conversations, messages, and contacts (website token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "CRISP_TOKEN"}
      }
    ]
  }
}
