{
  "name": "ramp",
  "type": "service",
  "title": "Ramp",
  "category": "Finance",
  "description": "Ramp transactions, reimbursements, cards, users, departments, locations, and business (OAuth)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "RAMP_ACCESS_TOKEN"}
      }
    ]
  }
}
