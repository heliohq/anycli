{
  "name": "sheets",
  "type": "service",
  "title": "Google Sheets",
  "category": "Productivity",
  "description": "Google Sheets as a tool (user access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "SHEETS_ACCESS_TOKEN"}
      }
    ]
  }
}
