{
  "name": "netsuite",
  "type": "service",
  "title": "NetSuite",
  "category": "Finance",
  "description": "NetSuite ERP as a tool — SuiteTalk REST records + SuiteQL (Token-Based Auth)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "credentials"},
        "inject": {"type": "env", "env_var": "NETSUITE_CREDENTIALS"}
      }
    ]
  }
}
