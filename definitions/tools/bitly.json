{
  "name": "bitly",
  "type": "service",
  "description": "Bitly as a tool (OAuth 2.0 access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "BITLY_ACCESS_TOKEN"}
      }
    ]
  }
}
