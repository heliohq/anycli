{
  "name": "drive",
  "type": "service",
  "description": "Google Drive as a tool (user access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "DRIVE_ACCESS_TOKEN"}
      }
    ]
  }
}
