{
  "name": "copper",
  "type": "service",
  "title": "Copper",
  "category": "Sales",
  "description": "Copper CRM as a tool (OAuth 2.0 access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "COPPER_ACCESS_TOKEN"}
      }
    ]
  }
}
