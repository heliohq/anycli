{
  "name": "tasks",
  "type": "service",
  "description": "Google Tasks as a tool — the user's personal task lists (user access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "GOOGLE_TASKS_ACCESS_TOKEN"}
      }
    ]
  }
}
