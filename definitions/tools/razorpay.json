{
  "name": "razorpay",
  "type": "service",
  "description": "Razorpay payments, refunds, customers, and payment links (OAuth 2.0 access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "RAZORPAY_ACCESS_TOKEN"}
      }
    ]
  }
}
