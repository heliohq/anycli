{
  "name": "moz",
  "type": "service",
  "title": "Moz",
  "category": "Analytics",
  "description": "Moz SEO data API (Domain Authority, backlinks, keyword metrics; JSON-RPC v2 token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "MOZ_API_TOKEN"}
      }
    ]
  }
}
