{
  "name": "calendar",
  "type": "service",
  "description": "Google Calendar as a tool (user access token)",
  "auth": {
    "credentials": [
      {
        "source": {"field": "access_token"},
        "inject": {"type": "env", "env_var": "CALENDAR_ACCESS_TOKEN"}
      }
    ]
  }
}
